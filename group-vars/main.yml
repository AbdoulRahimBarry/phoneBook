# variable vaulted

var_docker_hub_login: "{{ vault_docker_hub_login }}"
var_docker_hub_password: "{{ vault_docker_hub_password }}"
var_my_private_key: "{{ vault_my_private_key }}"

var_repository_github: "https://github.com/diranetafen/crud-application-using-flask-and-mysql.git"

# other variable
house: "/home/centos"

repository_brach: "master"
repository_tag: "1.0"
